1.  Why does the following expression 0.1 + 0.2 === 0.3 evaluate to false in Javascript?
    -   Because javascript cant handle floating point numbers very well so when adding 2 of them together javascript will aproximate the numbers witch doesnt make it 0.3
2.  What does the method attribute on a form element decide?
    -   It desides witch HTTP method to send the data with, GET or POST
3.  Explain why the form fields appear in the URL when the method on form element is changed to GET?
    -   This is because the GET method requests info from the server by sending it trough the url
4.  Why is it considered bad practice to only validate form input with HTML5 and/or Javascript?
    -   Because thsi can be changed by the user, meaning the restrictions can be bypassed, leaving the server open to attacks.
5.  When is it appropriate to use textarea over input?
    -   Textarea is usef or longer pieces of text, and will allow the user to add linebreaks, while input is made more for one line inputs, like a name or some small bit of information.